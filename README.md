```markdown
# Hybrid-IDS: SDN Intrusion Detection System using Soft-Label Stream Learning

An academic framework for a Hybrid Network Intrusion Detection System (Hybrid-IDS) designed for Software-Defined Networking (SDN) environments. The architecture bridges signature-based detection (Snort) with dynamic online machine learning (Streaming Hoeffding Tree) inside a POX controller, utilizing a **Probabilistic Knowledge Distillation (Soft-Label)** mechanism to suppress transient network noise and False Positives.

---

## 🚀 Key Features

- **Asynchronous Feedback Loop:** Implements a stateful `FeedbackHandler` that synchronizes rapid sub-millisecond model inferences with deeper, delayed inspection alerts from Snort without halting active live traffic.
- **Noise Mitigation via Soft Labels:** Employs a `SoftLabelHoeffdingTree` wrapper over `river` that dynamically adjusts trust ($\alpha$-decay) based on leaf maturity, protecting the model from early-stage false alerts.
- **Stateful Live Feature Extraction:** Translates raw OpenFlow packet streams dynamically into 41 full connection-based and windowed cross-traffic features compatible with the NSL-KDD space.
- **Automated Benchmarking:** Includes a programmatic evaluation wrapper for rigorous, reproducible multi-vector attack testing inside Mininet.

---

## 📂 Project Structure

| File / Folder | Purpose |
| :--- | :--- |
| `online_ids.py` | Core SDN controller script (Houses packet handlers, sliding flow windows, and policy enforcement). |
| `soft_tree.py` | Custom `SoftLabelHoeffdingTree` wrapper supporting probabilistic training weights. |
| `feature_extractor.py` | State tracker transforming live OpenFlow streams into windowed (2s) and host-based (100 conns) historical features. |
| `snort_agent.py` | Real-time agent translating raw signature hits into standardized JSON telemetry. |
| `run_benchmarks.py` | Automated testing engine driving deterministic multi-vector simulation phases. |
| `plot_metrics.py` | Statistical visualization script rendering runtime evaluation figures. |
| `All_Traffic_NSL-KDD.csv` | Full balanced network reference dataset (~490,000 samples) used for maturity baseline setup. |
| `model_rf.py` | Cold-start bootstrapping routine initializing the streaming classifier pipeline. |
| `trained_model2.pkl` | Serialized model pipeline snapshot containing standard scaling and encoder initial states. |
| `logs/` | Runtime telemetry repository tracking incremental evaluation metrics, raw flow predictions, and captured plots. |

---

## 🛠️ Prerequisites & Installation

### Environment Requirements
- **OS:** Linux (Ubuntu 20.04/22.04 LTS recommended)
- **SDN Controller:** POX (Python 3 compliant branch cloned into root or system path)
- **Emulator:** Mininet (configured with Open vSwitch `ovsk`)
- **IDS Engine:** Snort 2.9+

### Dependencies Setup
Install the necessary processing dependencies via `pip`:
```bash
pip install river pandas matplotlib
```

---

## 📋 Comprehensive Feature Mapping Table

| ID | NSL-KDD Feature Name | Data Type | Pipeline Extraction Logic / `feature_extractor.py` Implementation Reference | Feature Category & Analytical Context |
| :---: | :--- | :---: | :--- | :--- |
| **1** | `duration` | Continuous | Calculates elapsed flow time via `max(time.time() - self.start_time, 0.0)` in `FlowRecord`. | **Category A:** Basic Connection Attribute. Measures active session lifespan. |
| **2** | `protocol_type` | Symbolic | Extracted from L3/L4 packet matchers: `self.protocol_type` evaluates to `tcp`, `udp`, or `icmp`. | **Category A:** Network layer layer protocol verification. |
| **3** | `service` | Symbolic | Routed through `ServiceMapper.get()` via target port mapping or `ICMP_SERVICE_MAP` index. | **Category A:** Destination application layer classification. |
| **4** | `flag` | Symbolic | Generated by `FlagMapper.compute()` parsing internal TCP bit combinations (`SYN`, `FIN`, `RST`, `ACK`). | **Category A:** Connection status tracking flags (`SF`, `S0`, `REJ`). |
| **5** | `src_bytes` | Continuous | Incremented conditionally: `if ip.srcip == self.src_ip: self.src_bytes += len(raw_data)`. | **Category A:** Total volume payload sent out from source IP. |
| **6** | `dst_bytes` | Continuous | Incremented conditionally: `else: self.dst_bytes += len(raw_data)` on incoming packet updates. | **Category A:** Total volume payload delivered back from target IP. |
| **7** | `land` | Binary | Strict equality check evaluation: `if self.src_ip == self.dst_ip: self.land = 1`. | **Category A:** Flag targeting loopback exploits (Land Attack anomalies). |
| **8** | `wrong_fragment` | Continuous | Conditional increments: `if ip.frag != 0 or (ip.flags & 0x2): self.wrong_fragment += 1`. | **Category A:** Fragmentation error accounting counter. |
| **9** | `urgent` | Continuous | Incremented dynamically: `if f & 0x20: self.urg_count += 1` inside TCP control blocks. | **Category A:** Account tracking for out-of-band data delivery flags. |
| **10** | `hot` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Bypassed due to OpenFlow SDN real-time parsing latency bounds. |
| **11** | `num_failed_logins` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Requires Deep Packet Inspection (DPI) above flow-level. |
| **12** | `logged_in` | Binary | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** User session privilege status (Host-level scope). |
| **13** | `num_compromised` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Tracks host operating system stability state anomalies. |
| **14** | `root_shell` | Binary | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Administrative command execution visibility metrics. |
| **15** | `su_attempted` | Binary | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Superuser execution tracking indicators. |
| **16** | `num_root` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Root command execution visibility metrics. |
| **17** | `num_file_creations` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Host local filesystem modifications indicators. |
| **18** | `num_shells` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Active terminal sub-shell environment monitors. |
| **19** | `num_access_files` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Monitors access to system configuration configuration targets. |
| **20** | `num_outbound_cmds` | Continuous | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Outbound session command processing metrics. |
| **21** | `is_host_login` | Binary | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Identity authentication target host indicators. |
| **22** | `is_guest_login` | Binary | Statically locked dummy default: Hardcoded to `0` inside the `base_features` dictionary. | **Category Content:** Identity verification evaluating unprivileged access attempts. |
| **23** | `count` | Continuous | Evaluated as `count = len(same_dst)` inside the `window_features` calculation loop. | **Category B (Time-Based Window):** Total sessions matching this specific target IP in 2s. |
| **24** | `srv_count` | Continuous | Evaluated as `srv_count = len(same_srv)` inside the `window_features` calculation loop. | **Category B (Time-Based Window):** Total sessions matching this service port in 2s. |
| **25** | `serror_rate` | Continuous | Derived via ratio: `sum(1 for e in same_dst if is_serror(e)) / count` | **Category B (Time-Based Window):** Percentage of SYN-flag errors per destination host in 2s. |
| **26** | `srv_serror_rate` | Continuous | Derived via ratio: `sum(1 for e in same_srv if is_serror(e)) / srv_count` | **Category B (Time-Based Window):** Percentage of SYN-flag errors across target service in 2s. |
| **27** | `rerror_rate` | Continuous | Derived via ratio: `sum(1 for e in same_dst if is_rerror(e)) / count` | **Category B (Time-Based Window):** Percentage of RST-flag errors per destination host in 2s. |
| **28** | `srv_rerror_rate` | Continuous | Derived via ratio: `sum(1 for e in same_srv if is_rerror(e)) / srv_count` | **Category B (Time-Based Window):** Percentage of RST-flag errors across target service in 2s. |
| **29** | `same_srv_rate` | Continuous | Computed expression: `same_srv_in_dst / count` within `window_features`. | **Category B (Time-Based Window):** Proportion of matching services targeting this host. |
| **30** | `diff_srv_rate` | Continuous | Computed inverse expression: `1.0 - same_srv_rate` within `window_features`. | **Category B (Time-Based Window):** Proportion of conflicting service ports requested on host. |
| **31** | `srv_diff_host_rate` | Continuous | Computed expression: `srv_diff_host / srv_count` within `window_features`. | **Category B (Time-Based Window):** Target host distribution rate sharing a single service. |
| **32** | `dst_host_count` | Continuous | Evaluates to `total` which corresponds to `len(buf)` within `host_features`. | **Category C (Host-Based Historical):** Rolling window historical counter (max 100). |
| **33** | `dst_host_srv_count` | Continuous | Evaluates to `srv_tot` which corresponds to `len(same_srv)` within `host_features`. | **Category C (Host-Based Historical):** Matching service instances in historical host buffers. |
| **34** | `dst_host_same_srv_rate` | Continuous | Expression calculated via: `srv_tot / total` inside `host_features`. | **Category C (Host-Based Historical):** Target service distribution ratio in host history. |
| **35** | `dst_host_diff_srv_rate` | Continuous | Inverse expression calculated via: `1.0 - (srv_tot / total)` inside `host_features`. | **Category C (Host-Based Historical):** Target service conflict ratio in host history. |
| **36** | `dst_host_same_src_port_rate` | Continuous | Calculated via: `sum(1 for e in buf if e['src_port'] == src_port) / total`. | **Category C (Host-Based Historical):** Percentage of matching origin source ports to host. |
| **37** | `dst_host_srv_diff_host_rate` | Continuous | Advanced distinct mapping: `len(set(e['src_ip'] for e in same_srv)) / srv_tot`. | **Category C (Host-Based Historical):** Source IP structural diversity sharing a single target service. |
| **38** | `dst_host_serror_rate` | Continuous | Ratio evaluation: `sum(1 for e in buf if is_serror(e)) / total`. | **Category C (Host-Based Historical):** Historical global host SYN flag transmission error rate. |
| **39** | `dst_host_srv_serror_rate` | Continuous | Ratio evaluation: `sum(1 for e in same_srv if is_serror(e)) / srv_tot`. | **Category C (Host-Based Historical):** Historical target service scoped SYN flag transmission error rate. |
| **40** | `dst_host_rerror_rate` | Continuous | Ratio evaluation: `sum(1 for e in buf if is_rerror(e)) / total`. | **Category C (Host-Based Historical):** Historical global host RST flag transmission error rate. |
| **41** | `dst_host_srv_rerror_rate` | Continuous | Ratio evaluation: `sum(1 for e in same_srv if is_rerror(e)) / srv_tot`. | **Category C (Host-Based Historical):** Historical target service scoped RST flag transmission error rate. |

---

## 🛠️ Architectural Design Rationale (Defensive Thesis Note)

Features numbered **10 through 22** are classified as **Content Features** which historically track host-system telemetry parameters like configuration updates, authentication mechanisms, or file modifications. 

Because this architecture operates directly as an in-line **SDN Central Controller module (via OpenFlow)**, it evaluates traffic telemetry dynamically without executing intensive **Deep Packet Inspection (DPI)** workloads. Consequently, maintaining these specific attributes at a constant value of `0` represents a calculated engineering trade-off designed to guarantee **line-rate processing speeds, mitigate switch-controller latency, and prevent CPU starvation constraints** within real-time SDN control loops.
---

### Module Linkage

Deploy the central module file inside your active POX expansion workspace directory:

```bash
cp online_ids.py soft_tree.py feature_extractor.py /path/to/pox/pox/ext/
```

---

## 💻 Execution & Orchestration

The platform requires synchronized operation across separate service layers. Execute individual systems in the strict sequence mapped out below:

### Step 1: Boot the SDN Controller Core

Initiate the monitoring engine inside your central POX framework suite path:

```bash
sudo -E env PATH="$PATH" ./pox/pox.py openflow.of_01 online_ids
```

### Step 2: Spin Up the Mininet Topology

Emulate the open test-bed instance framework (Creates 3 Hosts, 1 OVS instance, mapped to the remote controller):

```bash
sudo mn -c
sudo mn --topo single,3 --mac --switch ovsk --controller remote
```

### Step 3: Instantiate the Snort Telemetry Agent

Mirroring interfaces are explicitly mapped. Spawn the alert parsing daemon on host `h3`:

```mininet
mininet> xterm h3
```

Inside the designated host window, kickstart the monitoring script:

```bash
sudo python3 snort_agent.py
```

### Step 4: Run Automated Attack Benchmarks

Inject multi-vector exploit profiles directly inside host `h1`'s isolated network context to test detection capabilities:

```mininet
mininet> xterm h1
```

Inside the attacker host window, run the benchmarking program:

```bash
sudo python3 run_benchmarks.py
```

---

## 📊 Evaluation & Metrics Visualization

The benchmarking engine will run 5 independent phases sequentially: Baseline Normal Traffic, TCP SYN Flood (`hping3`), Port Scanning (`nmap`), ICMP Flood (`ping -f`), and SSH Brute Force simulation (`nc`).

Once the validation test finishes, generate the thesis evaluation plots by running:

```bash
python3 plot_metrics.py
```

This updates the persistent state tracking statistics in the `logs/` directory and renders a high-fidelity evaluation plot detailing how the model scales under live adversarial scenarios:

### Metric Outputs Explained:

* **Precision / Recall Linearity:** Measures convergence across evolving streams. Rapid stabilization highlights highly effective cross-environment generalization.
* **FPR Stability:** A near-zero False Positive Rate validates that the **Soft-Labeling Knowledge Distillation** architecture prevents transient signature noise from destabilizing the adaptive Hoeffding tree structure.
* **Latency Overheads:** Tracks real-time statistical processing window limits (averaging $\sim$10 seconds), showing standard collection delays needed for building cross-traffic connection vectors without bottlenecking the fast data plane.
```
